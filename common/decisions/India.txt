RAJ_religious_groups = {
	RAJ_appease_hindus = {
		icon = GFX_decision_generic_political_discourse
		cost = 25

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_appease_hindus"
			set_temp_variable = { temp_change = 2 }
			RAJ_change_hindu_opinion = yes
			set_temp_variable = { temp_change = -1 }
			RAJ_change_muslim_opinion = yes
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				check_variable = { RAJ_hindus_opinion < 30 }
			}
			modifier = {
				add = 10
				check_variable = { RAJ_hindus_opinion < 15 }
			}
		}
	}

	RAJ_appease_sikhs = {
		icon = GFX_decision_generic_political_discourse
		cost = 25

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_appease_sikhs"
			set_temp_variable = { temp_change = 2 }
			RAJ_change_sikh_opinion = yes
			set_temp_variable = { temp_change = -1 }
			RAJ_change_hindu_opinion = yes
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				check_variable = { RAJ_sikh_opinion < 30 }
			}
			modifier = {
				add = 10
				check_variable = { RAJ_sikh_opinion < 15 }
			}
		}
	}

	RAJ_appease_buddhists = {
		icon = GFX_decision_generic_political_discourse
		cost = 25
		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_appease_buddhists"
			set_temp_variable = { temp_change = 2 }
			RAJ_change_buddhist_opinion = yes
			set_temp_variable = { temp_change = -1 }
			RAJ_change_hindu_opinion = yes
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				check_variable = { RAJ_buddhist_opinion < 30 }
			}
			modifier = {
				add = 10
				check_variable = { RAJ_buddhist_opinion < 15 }
			}
		}
	}

	RAJ_appease_christians = {
		icon = GFX_decision_generic_political_discourse
		cost = 25

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_appease_christians"
			set_temp_variable = { temp_change = 2 }
			RAJ_change_christian_opinion = yes
			set_temp_variable = { temp_change = -1 }
			RAJ_change_hindu_opinion = yes
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				check_variable = { RAJ_christians_opinion < 30 }
			}
			modifier = {
				add = 10
				check_variable = { RAJ_christians_opinion < 15 }
			}
		}
	}

	RAJ_appease_muslims = {
		icon = GFX_decision_generic_political_discourse
		cost = 25

		days_remove = 30

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_appease_muslims"
			set_temp_variable = { temp_change = 2 }
			RAJ_change_muslim_opinion = yes
			set_temp_variable = { temp_change = -1 }
			RAJ_change_hindu_opinion = yes
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 5
				check_variable = { RAJ_muslim_opinion < 30 }
			}
			modifier = {
				add = 10
				check_variable = { RAJ_muslim_opinion < 15 }
			}
		}
	}

	RAJ_ladakh_crisis = {
		icon = GFX_decision_crisis
		activation = {
			owns_state = 993
			check_variable = { RAJ_muslim_opinion < 15 }
			NOT = { has_country_flag = RAJ_ladakh_rebellion_fired }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = RAJ_raise_muslim_opinion_tt
				check_variable = { RAJ_muslim_opinion > 14 }
			}
		}

		cancel_trigger = {
			NOT = { owns_state = 993 }
		}

		days_mission_timeout = 180
		is_good = no
		selectable_mission = no
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_ladakh_crisis resolved"
			add_stability = 0.02
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_ladakh_crisis timed out"
			993 = { set_demilitarized_zone = yes }
			add_stability = -0.05
			country_event = { id = RAJ_rebellion.1 days = 90 }
		}
	}

	RAJ_meghalaya_crisis = {
		icon = GFX_decision_crisis
		activation = {
			owns_state = 980
			check_variable = { RAJ_christians_opinion < 15 }
			NOT = { has_country_flag = RAJ_meghalaya_rebellion_fired }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = RAJ_raise_christian_opinion_tt
				check_variable = { RAJ_christians_opinion > 14 }
			}
		}

		cancel_trigger = {
			NOT = { owns_state = 980 }
		}

		days_mission_timeout = 180
		is_good = no
		selectable_mission = no
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_meghalaya_crisis resolved"
			add_stability = 0.02
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_meghalaya_crisis timed out"
			980 = { set_demilitarized_zone = yes }
			add_stability = -0.05
			country_event = { id = RAJ_rebellion.2 days = 90 }
		}
	}

	RAJ_manipur_crisis = {
		icon = GFX_decision_crisis
		activation = {
			owns_state = 477
			check_variable = { RAJ_christians_opinion < 15 }
			NOT = { has_country_flag = RAJ_manipur_rebellion_fired }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = RAJ_raise_christian_opinion_tt
				check_variable = { RAJ_christians_opinion > 14 }
			}
		}

		cancel_trigger = {
			NOT = { owns_state = 477 }
		}

		days_mission_timeout = 180
		is_good = no
		selectable_mission = no
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_manipur_crisis resolved"
			add_stability = 0.02
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_manipur_crisis timed out"
			477 = { set_demilitarized_zone = yes }
			add_stability = -0.05
			country_event = { id = RAJ_rebellion.3 days = 90 }
		}
	}

	RAJ_sikkim_crisis = {
		icon = GFX_decision_crisis
		activation = {
			owns_state = 982
			check_variable = { RAJ_buddhist_opinion < 15 }
			NOT = { has_country_flag = RAJ_sikkim_rebellion_fired }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = RAJ_raise_buddhist_opinion_tt
				check_variable = { RAJ_buddhist_opinion > 14 }
			}
		}

		cancel_trigger = {
			NOT = { owns_state = 982 }
		}

		days_mission_timeout = 180
		is_good = no
		selectable_mission = no
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_sikkim_crisis resolved"
			add_stability = 0.02
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_sikkim_crisis timed out"
			982 = { set_demilitarized_zone = yes }
			add_stability = -0.05
			country_event = { id = RAJ_rebellion.4 days = 90 }
		}
	}

	RAJ_punjab_crisis = {
		icon = GFX_decision_crisis
		activation = {
			owns_state = 429
			check_variable = { RAJ_sikh_opinion < 15 }
			NOT = { has_country_flag = RAJ_punjab_rebellion_fired }
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = RAJ_raise_sikh_opinion_tt
				check_variable = { RAJ_sikh_opinion > 14 }
			}
		}

		cancel_trigger = {
			NOT = { owns_state = 429 }
		}

		days_mission_timeout = 180
		is_good = no
		selectable_mission = no
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_punjab_crisis resolved"
			add_stability = 0.02
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: RAJ_punjab_crisis timed out"
			429 = { set_demilitarized_zone = yes }
			add_stability = -0.05
			country_event = { id = RAJ_rebellion.5 days = 90 }
		}
	}
}

RAJ_Export_category = {
	RAJ_ita_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_italy_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_ita_export"
			ITA = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
				set_temp_variable = { treasury_change = 3 }
				modify_treasury_effect = yes
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}

	RAJ_shri_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_srilanka_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_shri_export"
			SRI = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}

	RAJ_isr_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_israeli_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_isr_export"
			ISR = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_sau_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_saudi_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_sau_export"
			SAU = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_arm_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_nardi_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_arm_export"
			ARM = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_eth_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_ethiopia_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_eth_export"
			ETH = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_nep_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_nepal_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_nep_export"
			NEP = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_fra_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_baguette_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_fra_export"
			FRA = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_sov_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_russia_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_sov_export"
			SOV = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_mld_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_mld_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_mld_export"
			MLD = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_mrt_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_mrt_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_mrt_export"
			MRT = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_egy_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_egy_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_egy_export"
			EGY = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_uae_export = {
		icon = GFX_decision_desicion_cat_weapon

		days_remove = 300

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		available = {
			has_country_flag = RAJ_uae_export_weapon_flag
		}

		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_uae_export"
			UAE = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
	RAJ_bhu_export = {
		icon = GFX_decision_desicion_cat_weapon
		days_remove = 300

		available = {
			has_country_flag = RAJ_bhu_export_weapon_flag
		}

		visible = {
			has_completed_focus = RAJ_indian_arms_exports
		}
		cost = 65

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_bhu_export"
			BHU = {
				country_event = { id = raj_exports.1 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_equipment_to_stockpile = {
					type = Anti_Air_0
					amount = 40
					producer = RAJ
				}
			}
		}
		ai_will_do = {
			base = 10
			modifier = {
				factor = 0
				has_war = yes
			}
		}
	}
}

RAJ_brics_alliance_decisions = {
	# Tier 2: Full Member Invite
	RAJ_brics_invite_full_member = {
		target_array = global.BRICS_potential

		target_root_trigger = {
			has_global_flag = BRICS_created
			has_idea = BRICS_leader
		}

		target_trigger = {
			FROM = { BRICS_valid_invite_target = yes }
		}

		available = {
			has_idea = BRICS_leader
			FROM = { BRICS_can_full_member = yes }
		}

		days_remove = 60
		fire_only_once = no
		days_re_enable = 270
		cost = 100

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_brics_invite_full_member target: [From.GetName]"
			add_to_variable = { global.BRICS_gui_dirty = 1 }
			FROM = {
				country_event = { id = indian_event.70 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_to_faction = FROM
				add_opinion_modifier = { target = FROM modifier = RAJ_brics_member_opinion }
			}
		}

		ai_will_do = {
			base = 20
			modifier = {
				factor = 1.5
				FROM = { has_opinion = { target = ROOT value > 50 } }
			}
		}
	}

	# Tier 3: Associate Member Invite
	RAJ_brics_invite_associate_member = {
		target_array = global.BRICS_potential

		target_root_trigger = {
			has_global_flag = BRICS_created
			has_idea = BRICS_leader
		}

		target_trigger = {
			FROM = { BRICS_valid_invite_target = yes }
		}

		available = {
			has_idea = BRICS_leader
			FROM = { BRICS_can_associate_member = yes }
		}

		days_remove = 45
		fire_only_once = no
		days_re_enable = 180
		cost = 75

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_brics_invite_associate_member target: [From.GetName]"
			add_to_variable = { global.BRICS_gui_dirty = 1 }
			FROM = {
				country_event = { id = indian_event.73 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_opinion_modifier = { target = FROM modifier = RAJ_brics_associate_opinion }
			}
		}

		ai_will_do = {
			base = 15
			modifier = {
				factor = 1.3
				FROM = { has_opinion = { target = ROOT value > 30 } }
			}
		}
	}

	# Tier 4: Observer State Invite
	RAJ_brics_invite_observer = {
		target_array = global.BRICS_potential

		target_root_trigger = {
			has_global_flag = BRICS_created
			has_idea = BRICS_leader
		}

		target_trigger = {
			FROM = { BRICS_valid_invite_target = yes }
		}

		available = {
			has_idea = BRICS_leader
			FROM = { BRICS_can_observer = yes }
		}

		days_remove = 30
		fire_only_once = no
		days_re_enable = 90
		cost = 40

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_brics_invite_observer target: [From.GetName]"
			add_to_variable = { global.BRICS_gui_dirty = 1 }
			FROM = {
				country_event = { id = indian_event.76 days = 1 }
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				add_opinion_modifier = { target = FROM modifier = RAJ_brics_observer_opinion }
			}
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 1.2
				FROM = { has_opinion = { target = ROOT value > 0 } }
			}
		}
	}

	# Tier Promotion: Observer → Associate
	RAJ_brics_promote_to_associate = {
		target_array = global.BRICS_observers

		target_root_trigger = {
			has_global_flag = BRICS_created
			has_idea = BRICS_leader
		}

		target_trigger = {
			FROM = {
				exists = yes
				has_country_flag = BRICS_observer_status
				NOT = { has_country_flag = BRICS_associate_status }
				NOT = { has_country_flag = BRICS_full_member_status }
			}
		}

		available = {
			has_idea = BRICS_leader
			FROM = { BRICS_can_associate_member = yes }
		}

		days_remove = 45
		fire_only_once = no
		days_re_enable = 90
		cost = 50

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_brics_promote_to_associate target: [From.GetName]"
			add_to_variable = { global.BRICS_gui_dirty = 1 }
			FROM = {
				clr_country_flag = BRICS_observer_status
				set_country_flag = BRICS_associate_status
			}
			remove_from_array = { global.BRICS_observers = FROM }
			add_to_array = { global.BRICS_associates = FROM }
			add_opinion_modifier = { target = FROM modifier = RAJ_brics_associate_opinion }
			FROM = {
				add_opinion_modifier = { target = ROOT modifier = RAJ_brics_associate_opinion }
			}
		}

		ai_will_do = {
			base = 10
		}
	}

	# Tier Promotion: Associate → Full Member
	RAJ_brics_promote_to_full = {
		target_array = global.BRICS_associates

		target_root_trigger = {
			has_global_flag = BRICS_created
			has_idea = BRICS_leader
		}

		target_trigger = {
			FROM = {
				exists = yes
				has_country_flag = BRICS_associate_status
				NOT = { has_country_flag = BRICS_full_member_status }
			}
		}

		available = {
			has_idea = BRICS_leader
			FROM = { BRICS_can_full_member = yes }
		}

		days_remove = 60
		fire_only_once = no
		days_re_enable = 90
		cost = 75

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_brics_promote_to_full target: [From.GetName]"
			add_to_variable = { global.BRICS_gui_dirty = 1 }
			FROM = {
				clr_country_flag = BRICS_associate_status
				set_country_flag = BRICS_full_member_status
			}
			remove_from_array = { global.BRICS_associates = FROM }
			add_to_array = { global.BRICS_full_members = FROM }
			add_to_faction = FROM
			add_opinion_modifier = { target = FROM modifier = RAJ_brics_member_opinion }
			FROM = {
				add_opinion_modifier = { target = ROOT modifier = RAJ_brics_member_opinion }
			}
		}

		ai_will_do = {
			base = 10
		}
	}
}

maoist_decision_category = {
	RAJ_MAO_rebellion = {
		priority = 100
		icon = GFX_decision_generic_nationalism
		days_mission_timeout = 365
		fire_only_once = no

		activation = {
			hidden_trigger = {
				NOT = { has_country_flag = RAJ_naxalite_mission_active }
				has_country_flag = RAJ_has_naxalite_states
				has_country_flag = RAJ_has_severe_naxalite_states
				check_variable = { party_pop_array^19 > 0.14 }
			}
		}

		available = {
			always = no
		}

		on_map_mode = map_only

		selectable_mission = no

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = RAJ_focus_mao_how_to_cancel
				OR = {
					emerging_communist_state_are_in_power = yes
					neutrality_neutral_communism_are_in_power = yes
					emerging_anarchist_communism_are_in_power = yes
				}
			}
		}

		is_good = no

		timeout_effect = {
			set_country_flag = RAJ_naxalite_mission_active
			set_temp_variable = { party_index = 19 }
			set_temp_variable = { party_popularity_increase = 0.05 }
			add_relative_party_popularity = yes
			add_stability = -0.05
			country_event = RAJ_mao_reb.4
			activate_mission = RAJ_MAO_rebellion_armed
		}
	}

	RAJ_MAO_rebellion_armed = {
		priority = 100
		icon = GFX_decision_generic_nationalism
		days_mission_timeout = 365
		fire_only_once = yes

		activation = {
			hidden_trigger = {
				always = no
			}
		}

		on_map_mode = map_only

		selectable_mission = no

		cancel_trigger = {
			custom_trigger_tooltip = {
				tooltip = RAJ_focus_mao_how_to_cancel
				OR = {
					emerging_communist_state_are_in_power = yes
					neutrality_neutral_communism_are_in_power = yes
					emerging_anarchist_communism_are_in_power = yes
				}
			}
		}

		is_good = no

		timeout_effect = {
			country_event = RAJ_mao_reb.5
		}
	}

	RAJ_maoist_suppression = {
		icon = {
			key = GFX_decision_com_rebel_highest
			trigger = { FROM = { has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency } } }
		}
		icon = {
			key = GFX_decision_com_rebel_high
			trigger = { FROM = { has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency_1 } } }
		}
		icon = {
			key = GFX_decision_com_rebel_medium
			trigger = { FROM = { has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency_2 } } }
		}
		icon = {
			key = GFX_decision_com_rebel_moderat
			trigger = { FROM = { has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency_3 } } }
		}
		icon = {
			key = GFX_decision_com_rebel_weak
			trigger = { FROM = { has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency_4 } } }
		}

		state_target = yes
		on_map_mode = map_and_decisions_view
		targets = { 456 459 991 989 451 452 447 458 988 470 }

		target_trigger = {
			FROM = { RAJ_state_has_any_naxalite = yes }
		}

		target_root_trigger = {
			has_country_flag = RAJ_has_naxalite_states
		}

		cost = 60
		fixed_random_seed = no
		fire_only_once = no
		days_remove = 15

		available = {
			NOT = { has_country_flag = RAJ_suppression_active }
		}

		complete_effect = {
			set_country_flag = RAJ_suppression_active
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_maoist_suppression"
			clr_country_flag = RAJ_suppression_active
			random_list = {
				75 = {
					set_temp_variable = { RAJ_naxalite_target_state = FROM }
					RAJ_downgrade_naxalite_var = yes
					RAJ_suppression_equipment_reward = yes
				}
				25 = {
					modifier = {
						factor = 1.5
						check_variable = { party_pop_array^19 > 0.09 }
					}
					modifier = {
						factor = 1.5
						check_variable = { party_pop_array^19 > 0.14 }
					}
					modifier = {
						factor = 1.5
						check_variable = { party_pop_array^19 > 0.19 }
					}
					custom_effect_tooltip = RAJ_suppression_failed_tt
					set_temp_variable = { party_index = 19 }
					set_temp_variable = { party_popularity_increase = 0.01 }
					add_relative_party_popularity = yes
					add_stability = -0.01
				}
			}
		}

		ai_will_do = { base = 25 }
	}

	RAJ_integrated_development_initiative = {
		icon = GFX_decision_generic_construction

		cost = 75
		fire_only_once = no
		days_remove = 60
		fixed_random_seed = no

		available = {
			NOT = {
				has_country_flag = RAJ_development_in_progress
			}
			has_country_flag = RAJ_has_naxalite_states
		}

		visible = {
			has_country_flag = RAJ_has_naxalite_states
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_integrated_development_initiative"
			set_country_flag = RAJ_development_in_progress
			set_temp_variable = { treasury_change = -5 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_integrated_development_initiative removed"
			clr_country_flag = RAJ_development_in_progress
			random_list = {
				70 = { # Success
					add_stability = 0.02
					random_controlled_state = {
						limit = {
							RAJ_state_has_any_naxalite = yes
						}
						RAJ_downgrade_naxalite_level = yes
						add_dynamic_modifier = {
							modifier = RAJ_development_boost
							days = 180
						}
					}
				}
				30 = { # Failure - funds misappropriated
					add_stability = -0.01
					set_temp_variable = { party_index = 19 }
					set_temp_variable = { party_popularity_increase = 0.02 }
					add_relative_party_popularity = yes
					custom_effect_tooltip = RAJ_suppression_failed_tt
				}
			}
		}

		ai_will_do = {
			base = 50
			modifier = {
				factor = 2
				has_political_power > 100
			}
		}
	}

	RAJ_surrender_rehabilitation_program = {
		icon = GFX_decision_generic_political_discourse

		cost = 50
		fire_only_once = no
		days_remove = 30
		fixed_random_seed = no

		available = {
			NOT = {
				has_country_flag = RAJ_rehabilitation_in_progress
			}
		}

		visible = {
			has_country_flag = RAJ_has_naxalite_states
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_surrender_rehabilitation_program"
			set_country_flag = RAJ_rehabilitation_in_progress
		}

		remove_effect = {
			clr_country_flag = RAJ_rehabilitation_in_progress
			random_list = {
				40 = { # Success - insurgents reintegrated
					add_stability = 0.02
					country_event = RAJ_naxal.12
				}
				35 = { # Partial failure - limited uptake
					add_stability = -0.01
					set_temp_variable = { party_index = 19 }
					set_temp_variable = { party_popularity_increase = 0.01 }
					add_relative_party_popularity = yes
					custom_effect_tooltip = RAJ_suppression_failed_tt
				}
				25 = { # Full failure - program exploited for regrouping
					add_stability = -0.02
					set_temp_variable = { party_index = 19 }
					set_temp_variable = { party_popularity_increase = 0.02 }
					add_relative_party_popularity = yes
					custom_effect_tooltip = RAJ_suppression_failed_tt
				}
			}
		}

		ai_will_do = {
			base = 40
		}
	}

	RAJ_operation_green_hunt = {
		icon = GFX_decision_generic_military_support

		cost = 100
		fire_only_once = no
		days_remove = 90
		fixed_random_seed = no

		available = {
			NOT = {
				has_country_flag = RAJ_green_hunt_active
			}
			OR = {
				date > 2009.1.1
				has_country_flag = RAJ_has_severe_naxalite_states
			}
		}

		visible = {
			has_country_flag = RAJ_has_severe_naxalite_states
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_operation_green_hunt"
			set_country_flag = RAJ_green_hunt_active
			add_ideas = RAJ_operation_green_hunt_active
		}

		remove_effect = {
			clr_country_flag = RAJ_green_hunt_active
			remove_ideas = RAJ_operation_green_hunt_active
			random_list = {
				75 = { # Success - major crackdown
					log = "[GetDateText]: [Root.GetName]: Operation Green Hunt succeeded"
					every_controlled_state = {
						limit = { RAJ_state_has_any_naxalite = yes }
						RAJ_downgrade_naxalite_level = yes
					}
					country_event = RAJ_naxal.15
				}
				25 = { # Failure - civilian casualties backfire
					log = "[GetDateText]: [Root.GetName]: Operation Green Hunt failed"
					add_stability = -0.03
					add_political_power = -25
					set_temp_variable = { party_index = 19 }
					set_temp_variable = { party_popularity_increase = 0.03 }
					add_relative_party_popularity = yes
					custom_effect_tooltip = RAJ_suppression_failed_tt
				}
			}
		}

		ai_will_do = {
			base = 80
			modifier = {
				factor = 0
				date < 2009.1.1
			}
		}
	}

	RAJ_samadhan_doctrine = {
		icon = GFX_decision_generic_intelligence_agency

		cost = 75
		fire_only_once = yes

		available = {
			date > 2017.1.1
			NOT = {
				has_idea = RAJ_samadhan_doctrine_idea
			}
		}

		visible = {
			date > 2015.1.1
			has_country_flag = RAJ_has_naxalite_states
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_samadhan_doctrine"
			add_ideas = RAJ_samadhan_doctrine_idea
		}

		ai_will_do = {
			base = 100
		}
	}

	# Salwa Judum - Controversial civilian militia program (2005-2011)
	RAJ_salwa_judum = {
		fixed_random_seed = no
		icon = GFX_decision_generic_prepare_civil_war_new

		cost = 40
		days_remove = 180

		available = {
			date > 2005.1.1
			date < 2012.1.1
			NOT = { has_idea = RAJ_salwa_judum_active }
			456 = { # Chhattisgarh
				is_controlled_by = RAJ
				OR = {
					has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency }
					has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency_1 }
					has_dynamic_modifier = { modifier = RAJ_naxalite_insurgency_2 }
				}
			}
		}

		visible = {
			date > 2004.1.1
			date < 2012.1.1
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_salwa_judum"
			add_stability = -0.02
			add_ideas = RAJ_salwa_judum_active
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_salwa_judum removed"
			set_temp_variable = { RAJ_naxalite_target_state = 456 }
			RAJ_downgrade_naxalite_var = yes
			random_list = {
				60 = { # Controversy
					add_stability = -0.03
					add_political_power = -25
				}
				40 = { # Success
					add_stability = 0.01
				}
			}
			remove_ideas = RAJ_salwa_judum_active
		}

		ai_will_do = {
			base = 50
		}
	}

	RAJ_road_connectivity_program = {
		icon = GFX_decision_generic_construction

		cost = 60
		days_remove = 120
		fixed_random_seed = no

		available = {
			num_of_civilian_factories > 30
		}

		visible = {
			has_country_flag = RAJ_has_naxalite_states
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_road_connectivity_program"
			set_temp_variable = { treasury_change = -8 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			random_list = {
				70 = { # Success - infrastructure completed
					random_controlled_state = {
						limit = {
							RAJ_state_has_any_naxalite = yes
						}
						add_dynamic_modifier = {
							modifier = RAJ_development_boost
							days = 180
						}
					}
					add_stability = 0.01
				}
				30 = { # Failure - funds embezzled, roads incomplete
					add_stability = -0.01
					set_temp_variable = { party_index = 19 }
					set_temp_variable = { party_popularity_increase = 0.01 }
					add_relative_party_popularity = yes
					custom_effect_tooltip = RAJ_suppression_failed_tt
				}
			}
		}

		ai_will_do = {
			base = 30
		}
	}
}

RAJ_central_asian_salaf_revolution = {
	#Kazakstan
	RAJ_revol_kaz = {
		icon = GFX_decision_generic_prepare_civil_war_new
		available = {
			KAZ = {
				NOT = {
					has_government = fascism
				}
				fascism > 0.3
			}
			custom_trigger_tooltip = {
				tooltip = is_top_influencer_kazaks
				KAZ = { RAJ_is_top_influencer = yes }
			}
		}
		cost = 200
		fire_only_once = yes
		days_remove = 70
		visible = {
			KAZ = {
				NOT = {
					has_government = fascism
				}
				communism > 0.15
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_revol_kaz"
			KAZ = {
				set_temp_variable = { rul_party_temp = 11 }
				set_temp_variable = { disable_elections = 1 }
				change_ruling_party_effect = yes

				set_temp_variable = { party_index = 11 }
				set_temp_variable = { party_popularity_increase = 0.25 }
				set_temp_variable = { temp_outlook_increase = 0.25 }
				add_relative_party_popularity = yes

				start_civil_war = {
					ideology = democratic
					size = 0.3
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	#Uzbekistan
	RAJ_revol_uzbekistan = {
		icon = GFX_decision_generic_prepare_civil_war_new
		available = {
			UZB = {
				NOT = {
					has_government = fascism
				}
				fascism > 0.3
			}
			custom_trigger_tooltip = {
				tooltip = is_top_influencer_uzbeki
				UZB = { RAJ_is_top_influencer = yes }
			}
		}
		cost = 200
		fire_only_once = yes
		days_remove = 70
		visible = {
			UZB = {
				NOT = {
					has_government = fascism
				}
				OR = {
					communism > 0.15
					neutrality > 0.15
					democratic > 0.15
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_revol_uzbekistan"
			UZB = {
				set_temp_variable = { rul_party_temp = 11 }
				set_temp_variable = { disable_elections = 1 }
				change_ruling_party_effect = yes

				set_temp_variable = { party_index = 11 }
				set_temp_variable = { party_popularity_increase = 0.25 }
				set_temp_variable = { temp_outlook_increase = 0.25 }
				add_relative_party_popularity = yes

				start_civil_war = {
					ideology = democratic
					size = 0.3
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	#Tajikistan
	RAJ_revol_taj = {
		icon = GFX_decision_generic_prepare_civil_war_new
		available = {
			TAJ = {
				NOT = {
					has_government = fascism
				}
				fascism > 0.3
			}
			custom_trigger_tooltip = {
				tooltip = is_top_influencer_tajiks
				TAJ = { RAJ_is_top_influencer = yes }
			}
		}
		cost = 200
		fire_only_once = yes
		days_remove = 70
		visible = {
			TAJ = {
				NOT = {
					has_government = fascism
				}
				OR = {
					communism > 0.15
					neutrality > 0.15
					democratic > 0.15
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_revol_taj"
			TAJ = {
				set_temp_variable = { rul_party_temp = 11 }
				set_temp_variable = { disable_elections = 1 }
				change_ruling_party_effect = yes

				set_temp_variable = { party_index = 11 }
				set_temp_variable = { party_popularity_increase = 0.25 }
				set_temp_variable = { temp_outlook_increase = 0.25 }
				add_relative_party_popularity = yes

				start_civil_war = {
					ideology = democratic
					size = 0.3
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	#Kyrgyzstan
	RAJ_revol_kyr = {
		icon = GFX_decision_generic_prepare_civil_war_new
		available = {
			KYR = {
				NOT = {
					has_government = fascism
				}
				fascism > 0.3
			}
			custom_trigger_tooltip = {
				tooltip = is_top_influencer_kyrgyzian
				KYR = { RAJ_is_top_influencer = yes }
			}
		}
		cost = 200
		fire_only_once = yes
		days_remove = 70
		visible = {
			KYR = {
				NOT = {
					has_government = fascism
				}
				OR = {
					communism > 0.15
					neutrality > 0.15
					democratic > 0.15
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_revol_kyr"
			KYR = {
				set_temp_variable = { rul_party_temp = 11 }
				set_temp_variable = { disable_elections = 1 }
				change_ruling_party_effect = yes

				set_temp_variable = { party_index = 11 }
				set_temp_variable = { party_popularity_increase = 0.25 }
				set_temp_variable = { temp_outlook_increase = 0.25 }
				add_relative_party_popularity = yes

				start_civil_war = {
					ideology = democratic
					size = 0.3
				}
			}
		}
		ai_will_do = { base = 0 }
	}
}

RAJ_greater_hindustan_decisions_category = {

	RAJ_expanding_sphere_of_influence = {
		icon = GFX_decision_afghanistan

		cost = 500
		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_expanding_sphere_of_influence"
			AFG = {
				country_event = indian_event.33
			}
			TAL = {
				country_event = indian_event.33
			}
			hidden_effect = {

				set_temp_variable = { wargoal_on = TAL }
				set_temp_variable = { wargoal_type = 2 }
				add_threat_from_wargoal_effect = yes
				create_wargoal = {
					type = annex_everything
					target = TAL
				}

				set_temp_variable = { wargoal_on = AFG }
				set_temp_variable = { wargoal_type = 2 }
				add_threat_from_wargoal_effect = yes
				create_wargoal = {
					type = annex_everything
					target = AFG
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = AFG transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = AFG
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_demand_sri_lanka = {
		icon = GFX_decision_eng_trade_unions_demand

		cost = 250

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_demand_sri_lanka"
			SRI = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = SRI }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = SRI
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = SRI transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = SRI
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_demand_myanma = {
		icon = GFX_decision_spr_political_assassination

		cost = 500

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_demand_myanma"
			BRM = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = BRM }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = BRM
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = BRM transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = BRM
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_burn_down_the_separatist_sentiments = {
		icon = GFX_decision_oppression

		cost = 250

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_burn_down_the_separatist_sentiments"
			KAC = {
				country_event = indian_event.33
			}
			WAA = {
				country_event = indian_event.33
			}
			KAR = {
				country_event = indian_event.33
			}
			SHN = {
				country_event = indian_event.33
			}
			NHN = {
				country_event = indian_event.33
			}

			add_threat = 0.05
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = KAC
				}
				create_wargoal = {
					type = annex_everything
					target = WAA
				}
				create_wargoal = {
					type = annex_everything
					target = SHN
				}
				create_wargoal = {
					type = annex_everything
					target = NHN
				}
				create_wargoal = {
					type = annex_everything
					target = KAR
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = KAC transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = KAC
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_land_of_freedom = {
		icon = GFX_decision_hol_war_on_pacifism

		cost = 500

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_land_of_freedom"
			SIA = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = SIA }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = SIA
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = SIA transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = SIA
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_laotian_resorts = {
		icon = GFX_decision_3si_dec

		cost = 250

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_laotian_resorts"
			LAO = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = LAO }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = LAO
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = LAO transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = LAO
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_vietnamese_intervention = {
		icon = GFX_decision_kavkaz_revol

		cost = 250

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_vietnamese_intervention"
			VIE = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = VIE }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = VIE
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = VIE transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = VIE
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_cambodian_natural_resources = {
		icon = GFX_decision_generic_construction

		cost = 250

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_cambodian_natural_resources"
			CBD = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = CBD }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = CBD
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = CBD transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = CBD
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_change_the_malaysian_constitution = {
		icon = GFX_decision_generic_construction

		cost = 250

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_change_the_malaysian_constitution"
			MAY = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = MAY }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = MAY
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = MAY transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = MAY
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_spank_singapore = {
		icon = GFX_decision_singapore

		cost = 250

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_spank_singapore"
			SIN = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = SIN }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = SIN
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = SIN transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = SIN
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
	RAJ_bring_hinduism_back_to_indonesia = {
		icon = GFX_decision_indonesia_export

		cost = 500

		days_remove = 120
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RAJ_bring_hinduism_back_to_indonesia"
			IND = {
				country_event = indian_event.33
			}
			ACE = {
				country_event = indian_event.33
			}

			set_temp_variable = { wargoal_on = IND }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes

			set_temp_variable = { wargoal_on = ACE }
			set_temp_variable = { wargoal_type = 2 }
			add_threat_from_wargoal_effect = yes
			hidden_effect = {
				create_wargoal = {
					type = annex_everything
					target = IND
				}
				create_wargoal = {
					type = annex_everything
					target = ACE
				}
			}
			custom_effect_tooltip = TT_IF_THEY_ACCEPT
			effect_tooltip = {
				RAJ = { annex_country = { target = IND transfer_troops = yes } }
			}
			custom_effect_tooltip = TT_IF_THEY_REJECT
			effect_tooltip = {
				RAJ = {
					create_wargoal = {
						type = annex_everything
						target = IND
					}
				}
			}
		}
		ai_will_do = { base = 0 }
	}
}
